[project]
name = "ingest"
version = "1.0.0"
description = "Add your description here"
requires-python = ">=3.13"
authors = [
    { name = "Haruna Musoke", email = "musokeharuna90@gmail.com" },
]
readme = "README.md"
dependencies = [
    "fastapi>=0.124.2",
    "inngest>=0.5.13",
    "uvicorn>=0.38.0",
]

[project.scripts]
ingest = "ingest.__main__:app"
cli = "ingest_cli.__main__:main"

[project.urls]
repository = "https://github.com/yotv-aardvark/outpost-trench.git"
issues = "https://github.com/yotv-aardvark/outpost-trench/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.coverage.report]
fail_under = 100
omit = ["__main__.py"]

[tool.coverage.run]
source = ["ingest"]

[tool.hatch.build.targets.sdist]
include = ["src"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
